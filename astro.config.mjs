import { defineConfig } from 'astro/config';

// GitHub Pages project site: https://arcadewise.github.io/althistdocs
// If the repo gets a custom domain later, set base to '/' and update site.
export default defineConfig({
  site: 'https://arcadewise.github.io',
  base: '/althistdocs',
});
